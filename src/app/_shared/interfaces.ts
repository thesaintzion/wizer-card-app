export interface Book {
    createdAt: string,
    name: string,
    isFvorite: boolean,
    id: string
}

export interface Category {
    createdAt: string,
    name: string,
    isFavorite: boolean,
    id: string
}
